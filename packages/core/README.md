# `@combric/core`

This package is a reserved, empty repository boundary. It has no supported
public API and is deliberately excluded from the Combric 1.0 public release set.
It is marked `private` so workspace tooling cannot publish it accidentally.

Do not install or depend on this package. Public Combric contracts are provided
by the packages listed in the [repository README](../../README.md#packages).
